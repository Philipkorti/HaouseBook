<UserControl x:Class="HaouseBook.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HaouseBook.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50px"/>
            <RowDefinition/>
            <RowDefinition Height="50px"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="#587792"/>
        <Rectangle Grid.Row="2" Fill="#587792"/>
        <StackPanel Orientation="Horizontal">
            <Button Width="100" Background="Transparent">Overview</Button>
            <Button Width="100" Background="Transparent">Transaction</Button>
        </StackPanel>
    </Grid>
</UserControl>
